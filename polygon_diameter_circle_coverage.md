# n边形最远点直径圆覆盖判定

## 1 问题定义

给定一个有界简单多边形，其顶点为

\[
V_0,V_1,\ldots,V_{n-1}\in\mathbb R^2,
\]

从多边形中选取距离最远的两个点 \(P,Q\)，以线段 \(PQ\) 为直径作圆。需要判断该圆能否覆盖整个多边形，包括其边界和内部。

本判定适用于凸多边形和凹多边形。顶点应为有限坐标，允许顺时针或逆时针输入；首尾顶点不应重复。若输入来自半平面交，建议先去除相邻重复点和共线冗余点。

## 2 核心结论

### 2.1 最远点可以从顶点中寻找

多边形包含于其所有顶点的凸包。欧氏距离在两组凸组合上的最大值可以在凸包的极点处取得，因此多边形的直径满足

\[
D=\max_{0\le i<j<n}\|V_i-V_j\|.
\]

也就是说，只需检查顶点对，不需要在每条边上连续搜索。对于凹多边形，真正参与最远点计算的顶点一定属于其凸包；直接枚举全部顶点仍然正确。

### 2.2 覆盖整个多边形只需检查全部顶点

圆盘是凸集。如果它包含所有顶点，就包含顶点的凸包，从而包含整个多边形。反过来，只要有一个顶点位于圆外，就不可能覆盖整个多边形。

设选中的最远点为 \(P,Q\)，圆心与半径为

\[
O=\frac{P+Q}{2},\qquad R=\frac{\|P-Q\|}{2}.
\]

对任意顶点 \(X\)，有恒等式

\[
(X-P)\cdot(X-Q)=\|X-O\|^2-R^2.
\]

因此，顶点 \(X\) 位于直径圆内或圆上，当且仅当

\[
(X-P)\cdot(X-Q)\le0.
\]

最终判定为

\[
\boxed{\max_i (V_i-P)\cdot(V_i-Q)\le0.}
\]

该点积判据不需要开平方，适合直接编程。

### 2.3 最远点对不唯一时如何处理

精确数学下，任选一组最远点对即可判定，不需要逐组尝试。

证明如下。假设某组最远点 \(P,Q\) 的距离为 \(D\)，而另一组最远点 \(A,B\) 所生成、半径为 \(D/2\) 的圆能够覆盖多边形。该圆必然同时包含 \(P,Q\)。设圆心为 \(O\)，则

\[
D=\|P-Q\|\le\|P-O\|+\|O-Q\|\le D.
\]

两个不等式只能同时取等号，所以 \(O\) 必为 \(PQ\) 的中点。因此，任何能够覆盖多边形的最远点直径圆都必须与 \(PQ\) 的直径圆相同。

由此得到：若任选的一组最远点生成的圆不能覆盖，则其他最远点对生成的圆也不能覆盖；若能够覆盖，则所有最远点对对应的是同一个覆盖圆。程序只需选取一组最大距离点对，但应使用容差避免浮点数造成错误换组。

## 3 直接算法

```text
输入：n个多边形顶点 V[0],...,V[n-1]

1. 检查 n≥3，所有坐标均为有限数值。
2. 枚举所有 i<j，计算距离平方
       d2(i,j) = ||V[i]-V[j]||²。
3. 取距离平方最大的一组顶点 P,Q。
4. 对每个顶点 X 计算
       value(X) = dot(X-P, X-Q)。
5. 若所有 value(X)≤数值容差，则覆盖；
   否则不覆盖，并返回所有圆外顶点。
```

直接算法的时间复杂度为 \(O(n^2)\)，空间复杂度为 \(O(1)\)，不计输出的圆外顶点列表。定位区域的顶点数量通常较少，应优先采用此算法作为正式实现和复核基准。

若顶点很多，可先以 \(O(n\log n)\) 求凸包，再对凸包使用旋转卡壳求直径；最后仍按点积公式检查顶点。优化版本应与直接枚举版本进行随机交叉验证。

## 4 Python参考实现

```python
from math import hypot, isfinite


def diameter_circle_covers_polygon(vertices, rel_tol=1e-10):
    """判断多边形的最远点直径圆是否覆盖整个多边形。

    参数
    ----
    vertices:
        [(x0, y0), ..., (x_{n-1}, y_{n-1})]。首尾点不要重复。
    rel_tol:
        相对于直径平方的无量纲容差。

    返回
    ----
    包含最远点下标、圆心、半径、覆盖结论、圆外顶点下标和
    最大点积违反量的字典。
    """
    if len(vertices) < 3:
        raise ValueError("多边形至少需要3个顶点")

    points = [(float(x), float(y)) for x, y in vertices]
    if not all(isfinite(x) and isfinite(y) for x, y in points):
        raise ValueError("顶点坐标必须是有限数值")

    best_i, best_j = 0, 1
    max_d2 = -1.0

    for i in range(len(points)):
        xi, yi = points[i]
        for j in range(i + 1, len(points)):
            xj, yj = points[j]
            d2 = (xi - xj) ** 2 + (yi - yj) ** 2
            if d2 > max_d2:
                max_d2 = d2
                best_i, best_j = i, j

    if max_d2 == 0.0:
        raise ValueError("所有顶点重合，输入不是有效多边形")

    px, py = points[best_i]
    qx, qy = points[best_j]
    center = ((px + qx) / 2.0, (py + qy) / 2.0)
    radius = hypot(px - qx, py - qy) / 2.0

    # 点积和距离平方单位相同，因此容差按直径平方缩放。
    dot_tol = rel_tol * max(1.0, max_d2)
    outside = []
    max_violation = float("-inf")

    for i, (x, y) in enumerate(points):
        value = (x - px) * (x - qx) + (y - py) * (y - qy)
        max_violation = max(max_violation, value)
        if value > dot_tol:
            outside.append(i)

    return {
        "diameter_pair_indices": (best_i, best_j),
        "diameter_pair": (points[best_i], points[best_j]),
        "diameter": 2.0 * radius,
        "center": center,
        "radius": radius,
        "covers": len(outside) == 0,
        "outside_vertex_indices": outside,
        "max_dot_violation": max_violation,
        "dot_tolerance": dot_tol,
    }
```

这里的`outside_vertex_indices`用于定位失败原因。如果结果为不覆盖，应在论文或调试输出中给出至少一个圆外顶点，而不只输出布尔值。

## 5 数值与输入注意事项

1. 比较距离时使用距离平方，避免不必要的开方。
2. 覆盖判定中的点积量纲为坐标单位的平方。容差也必须按距离平方缩放，不能直接照搬角度或长度容差。
3. 若输入坐标量级很大而多边形很小，可先平移顶点，使其均值或某个顶点位于原点，以减少浮点消减误差；平移不改变判定结果。
4. 若多边形由数值半平面交生成，应先按统一容差去除重复顶点，并保持原始未舍入坐标参与判定。
5. 圆上点应判定为被覆盖，因此使用`≤`而不是`<`。
6. 自交、所有点共线、重复点等无效或退化输入，应由调用方明确处理。对于线段退化区域，同一判据仍成立，但不再称为有效n边形。

## 6 测试样例

### 6.1 矩形：覆盖

顶点为`(0,0),(2,0),(2,1),(0,1)`。最远点为任一对角顶点，直径圆是矩形外接圆，四个顶点均在圆上，结果为覆盖。

### 6.2 最长边作直径：不覆盖

顶点依次为

```text
(-1,0), (1,0), (0.4,1.4), (-0.4,1.4)
```

唯一最远点对为前两个顶点，距离为2，且它们是一条边的端点。对应圆心为原点、半径为1；顶点`(0.4,1.4)`到圆心距离大于1，结果为不覆盖。该例同时说明最远点不一定为对角顶点，直径圆也不保证覆盖。

### 6.3 一般凸四边形：覆盖

顶点按边界依次为`(0,0),(2,0),(2,1),(0,0.5)`。最远点为`(0,0)`与`(2,1)`；另外两个顶点均在对应直径圆内或圆上，结果为覆盖。该测试也用于提醒：测试顶点必须按真实多边形边界组织，不能把无序点集直接称为多边形。

### 6.4 建议自动化性质测试

- 随机平移、旋转和统一缩放后，覆盖结论应不变。
- 参考点积判据与“顶点到圆心的距离平方不超过半径平方”应完全一致。
- 对凸包顶点计算的直径应与对全部原始顶点枚举的直径一致。
- 把任意圆内顶点插入一条边后，若不改变多边形直径，原覆盖结论不应由真变假。

## 7 在B题中的使用方式

第一问的定位区域是多个角度半平面的交，因此在非空且有界时为凸多边形。得到有序顶点后，直接调用上述算法即可回答“以定位区域直径为直径的圆能否覆盖定位区域”。

该判定仅回答题目中的直径圆覆盖问题。后续机器狗清除时，应计算定位区域的最小包围圆或另一个经过逐顶点验证的覆盖圆，并在覆盖半径不超过20m且留有数值裕量时清除。不能因为多边形直径不超过40m，就直接认为存在一个半径20m的安全清除圆。
