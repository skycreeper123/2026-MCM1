# 2026 年 B 题多 Agent 协作角色安排

## 1. 文档用途

本文档用于组织多个 Agent 协作完成 2026 年数学建模国赛 B 题“无线电干扰源的快速自动定位与清除”。团队需要同时处理计算几何、主动探测、在线路径决策、定向源搜索、模拟器通信、批量实验和论文表达。

本安排追求四个目标：

1. 每个 Agent 只获取完成本职工作所需的最少上下文；
2. 几何、策略、接口和验证相互分离，防止一个错误同时污染全部结果；
3. 问题三至少保留两条独立策略路线，第一版完成前不得互相模仿；
4. 正式测试前必须通过盲测、代码冻结和运行手册检查，不把有限的正式测试机会用于调试。

Agent A 是总协调者，只负责拆解、派工、状态管理、证据审查、冲突裁决和正式测试决策。Agent A 不推导定位公式，不设计搜索策略，不编写程序，不操作模拟器完成测试，也不修改其他 Agent 的实验结果。

## 2. B 题的共同任务定义

B 题包含四个相互衔接但性质不同的问题：

- 问题一：把带有正负 1 度误差的多个示向度转换成定位多边形，计算区域直径，并判断直径为该值的圆能否覆盖定位区域；
- 问题二：根据第一次示向度选择第二个检测点，使后续定位区域尽可能小，同时考虑移动距离和可能接收不到信号的风险；
- 问题三：面对 10 至 16 个频道互异的全向干扰源，设计机器狗的频道发现、定位、路径、清除和停止策略；
- 问题四：加入定向干扰源后重新设计策略，处理“无信号不等于没有目标”的困难。

官方评价首先要求尽可能清除全部干扰源，然后比较平均定位清除时间和程序运行时间。因此团队选择方案时采用分层顺序：

1. 全部清除成功率；
2. 最坏情况和高分位耗时；
3. 平均定位清除时间；
4. 程序稳定性；
5. 模型解释性和论文创新性。

不得用较短的平均时间掩盖漏掉干扰源的问题。

## 3. 团队结构

| Agent | 角色 | 核心职责 | 独立性要求 |
|---|---|---|---|
| A | 总协调与测试决策 | 拆题、派工、维护契约、组织盲测、控制正式测试机会 | 不推导、不编码、不运行正式测试 |
| B | 几何定位与选点理论 | 完成问题一、问题二的数学定义、算法和边界情况 | 不负责 HTTP 接口和全局搜索策略 |
| C | 模拟器接口与运行工程 | 封装四类接口、计时、重试、日志、运行手册 | 不决定搜索路线，不修改策略目标 |
| D | 问题三主策略 | 独立设计全向源发现、定位、清除和停止策略 | 第一版完成前不读取 E 的方案 |
| E | 问题三备选策略 | 用结构不同的思想独立完成问题三 | 第一版完成前不读取 D 的方案 |
| F | 问题四定向源策略 | 处理定向覆盖、无信号歧义、边界朝外发射和安全回退 | 不把 D 的假设直接当作结论 |
| G | 离线仿真与基准评测 | 建立影子模拟器、生成案例、维护封闭测试集、批量评测 | 不参与候选策略的首次设计 |
| H | 独立验证与红队 | 查找几何反例、漏源情形、停止错误、接口故障和时间账错误 | 不参与被验证代码的首次开发 |
| I | 论文、图表与提交 | 用已核验证据组织论文、正式结果、日志和 AI 使用记录 | 不创造数字，不替模型作者补结果 |

若可用 Agent 数量不足，可以合并 I 与 A 的材料管理工作，也可以让 B 在完成几何任务后帮助审阅论文公式。不得合并以下角色：

- D 与 E；
- C 与 H；
- 候选策略作者 D/E/F 与封闭测试集所有者 G。

## 4. 目录和文件所有权

建议使用以下目录结构：

```text
26_CUMCM/
├─ 00_coord/                         # Agent A
│  ├─ TASK_BOARD.md
│  ├─ DECISION_LOG.md
│  ├─ OPEN_ISSUES.md
│  ├─ HANDOFF_INDEX.md
│  └─ FORMAL_TEST_LEDGER.md
├─ 01_geometry/                      # Agent B
├─ 02_simulator_interface/           # Agent C
├─ 03_q3_primary/                    # Agent D
├─ 04_q3_alternative/                # Agent E
├─ 05_q4_directional/                # Agent F
├─ 06_benchmark/                     # Agent G
│  ├─ public_cases/
│  └─ sealed_cases/                  # D/E/F 不读取
├─ 07_validation/                    # Agent H
├─ 08_paper_submission/              # Agent I
├─ shared/
│  ├─ PROBLEM_CONTRACT.md
│  ├─ PROTOCOL_CONTRACT.md
│  ├─ GEOMETRY_CONTRACT.md
│  ├─ POLICY_INTERFACE.md
│  ├─ METRIC_CONTRACT.md
│  ├─ RESULT_REGISTRY.json
│  └─ CLAIM_EVIDENCE.csv
└─ final/
```

文件规则：

1. 原始题面、附件、模拟器和正式日志保持只读；
2. 一个文件只有一个所有者，其他 Agent 通过问题单提出修改意见；
3. D、E 第一版冻结前不得读取对方的模型说明、代码、结果或提示词；
4. G 的封闭测试案例、随机种子和真值不得提供给 D、E、F；
5. C 只提供稳定接口，不在公共接口中硬编码某条策略；
6. I 只能从 `RESULT_REGISTRY.json` 和 `CLAIM_EVIDENCE.csv` 引用数字；
7. A 只能修改协调目录和任务状态，不进入模型目录修复代码；
8. 所有随机算法必须记录种子，所有实验必须有一条明确复现命令；
9. 正式测试日志不得改名、解密、删改或覆盖。

## 5. 最小上下文协议

不要向任何 Agent 转发完整聊天记录。启动任务包只包含：

1. 本文档中对应角色的任务卡；
2. B 题题面及与其职责直接相关的附件；
3. 最新版共享契约；
4. 不超过 800 字的必要上游摘要；
5. 明确的输出目录、验收标准和检查点；
6. 明确列出的禁止读取目录。

每个 Agent 完成一个阶段后只向 A 返回 `SUMMARY.md` 和文件索引。摘要控制在 800 字左右，并统一回答：

- 本阶段解决了什么；
- 使用了哪些输入和契约版本；
- 产生了哪些可复现结论；
- 哪些假设尚未验证；
- 下游只需读取哪些文件；
- 当前需要哪个角色回答哪个唯一问题。

代码、长推导、完整日志和大批量结果保留在角色目录，不粘贴进交接消息。

## 6. 共享契约

### 6.1 问题契约 `PROBLEM_CONTRACT.md`

由 B 起草，A 组织 D、E、F、H 审核。每一问必须写清：

- 输入、输出和最终交付物；
- 已知量、隐藏量、决策变量和状态变量；
- 每次动作前实际可获得的信息；
- 示向度、有效接收半径、定向方向和清除半径的定义；
- 全向与定向情形下 `no_signal` 的不同含义；
- 官方指标和内部指标；
- 必须证明或通过实验支持的结论；
- 与其他小问共享的接口和几何对象。

契约冻结前，所有策略结果只能标为探索性。后续改变题意解释时，A 必须记录版本和受影响实验。

### 6.2 协议契约 `PROTOCOL_CONTRACT.md`

由 C 维护、H 独立复核，至少包含：

- `/enter`、`/measure`、`/clear`、`/exit` 的字段和返回值；
- `request_id` 的生成、幂等重试和冲突规则；
- `accepted=false`、HTTP 错误和连接关闭的处理；
- 当前坐标、当前测向频道和虚拟时钟的状态更新规则；
- 移动、检测、切换频道、成功清除、失败清除的计时公式；
- 现实运行截止和虚拟时间截止；
- 日志字段、日志大小和正式日志保存规则。

策略只允许通过统一客户端调用模拟器，不得各自编写不一致的请求代码。

### 6.3 几何契约 `GEOMETRY_CONTRACT.md`

由 B 维护、H 审核，统一定义：

- 角度方向和角度归一化；
- 正负 1 度扇形约束；
- 定位区域、顶点排序、面积和直径；
- 最小包围圆及其数值容差；
- 空集、无界、近似平行和退化多边形；
- “可以保证在 20 米内清除”的判定条件；
- 几何函数的输入输出格式。

D、E、F 必须调用同一经过验证的几何模块，不能各自复制一份后静默修改。

### 6.4 策略接口 `POLICY_INTERFACE.md`

策略与模拟器通过统一抽象层通信。策略输入只包含当前可知状态，输出只能是下一动作或退出决定。建议统一数据结构：

```text
observation: 当前坐标、当前测向频道、虚拟时间、剩余现实时间、历史响应
action: enter / measure(position, channel) / clear(position, channel) / exit
channel_state: unknown / detected / locating / ready_to_clear / cleared / absent_certified
```

离线仿真和正式模拟器都实现同一接口，从而使策略代码不依赖具体运行环境。

### 6.5 指标契约 `METRIC_CONTRACT.md`

官方主指标：

```text
clear_ratio = cleared_sources / total_sources
average_clear_time = total_virtual_time / cleared_sources
program_runtime = exit_time - successful_enter_time
```

虚拟时间必须满足独立对账公式：

```text
total_virtual_time
= total_distance / 5
+ 5 * measure_count
+ 1 * channel_switch_count
+ 5 * successful_clear_count
+ 3 * failed_clear_count
```

内部评测至少包含：

- 全部清除成功率；
- 平均、中位数、P90、P95 和最大平均清除时间；
- 总移动距离、检测次数、频道切换次数；
- 第一次清除成功率和失败清除次数；
- 每个频道从发现到清除的用时；
- 漏源数和错误判定频道不存在的次数；
- 定位区域直径、面积和最小包围圆半径；
- API 错误、超时、异常退出和日志大小；
- 相对安全基线的时间改善比例。

模型选择采用分层比较，不把不同指标随意加权成一个总分。若内部调参需要标量损失，应给漏源设置大于任何可节省时间的惩罚，并保留原始分项指标。

### 6.6 结果注册表和结论证据表

`RESULT_REGISTRY.json` 建议记录：

```text
run_id,policy_version,simulator_version,case_set,seed,question,
full_clear,clear_ratio,total_virtual_time,average_clear_time,
distance,measure_count,switch_count,failed_clear_count,
program_runtime,exit_reason,log_path,code_hash
```

`CLAIM_EVIDENCE.csv` 建议字段：

```text
claim_id,claim_text,question_id,owner,evidence_file,reproduce_command,
validation_status,reviewer,limitations,paper_location
```

论文中的每个关键数字必须有 `claim_id`。未经 H 核验的结果不得写成确定结论。

## 7. 各 Agent 任务卡

### Agent A：总协调与正式测试决策

**目标**

让几何、接口、候选策略、仿真和验证在统一契约下协作，并确保正式测试只运行冻结且已通过盲测的版本。

**必须完成**

- 初始化任务板、决策日志、问题列表、交接索引和正式测试台账；
- 给每个 Agent 发送最小上下文任务包；
- 冻结问题、协议、几何、策略接口和指标契约；
- 维护 D/E 的盲隔离状态；
- 发现方案冲突时交给 G/H 设计判别实验，不凭直觉选择；
- 记录主策略、保底策略、回退条件和淘汰理由；
- 正式测试前核对代码哈希、配置、运行手册和剩余次数；
- 将已验证 `claim_id` 分配给 I。

**禁止事项**

- 不推导定位公式；
- 不设计检测点、路径或频道顺序；
- 不编写或调试策略代码；
- 不修改测试结果；
- 不亲自操作正式测试；
- 不因为某个 Agent 语言自信、算法复杂或图表漂亮而选方案。

**决策优先级**

1. 全部清除成功率；
2. 无协议错误且可稳定退出；
3. P95 和最坏情况时间；
4. 平均清除时间；
5. 证据完整、结果可复现；
6. 模型简洁和论文创新性。

**输出**

- `00_coord/TASK_BOARD.md`
- `00_coord/DECISION_LOG.md`
- `00_coord/OPEN_ISSUES.md`
- `00_coord/HANDOFF_INDEX.md`
- `00_coord/FORMAL_TEST_LEDGER.md`
- `00_coord/FORMAL_TEST_DECISION.md`

**完成标准**

每个进入正文或正式测试的结论都有负责人、代码版本、证据路径、复现命令和独立验证状态；每次正式测试均能追溯到审批记录和代码哈希。

### Agent B：几何定位与第二检测点理论

**目标**

为问题一、问题二以及后续策略提供统一、可靠、可证明的计算几何基础。

**任务**

- 将每个示向度表示为带正负 1 度误差的扇形或半平面约束；
- 建立多个测向约束的交集算法；
- 计算凸多边形直径并处理退化情况；
- 回答直径圆是否覆盖，并给出必要的反例或判定条件；
- 建立最小包围圆算法和 20 米保证清除判据；
- 为问题二定义“定位效果好”的量化目标；
- 给出第二检测点候选区域和稳健选点算法；
- 分析交会角、测量基线、接收半径和移动时间的关系；
- 提供极端角度、跨 0 度、近平行和空交集测试用例。

**禁止事项**

- 不负责全局频道搜索；
- 不编写 HTTP 请求；
- 不根据某次模拟器结果改变数学定义；
- 不把两条示向度中心线的交点当作无误差真值。

**输出**

- `01_geometry/Q1_MODEL.md`
- `01_geometry/Q2_MODEL.md`
- `shared/GEOMETRY_CONTRACT.md`
- `01_geometry/GEOMETRY_TEST_CASES.json`
- 几何模块、测试和图形示例。

**交叉验证**

H 独立构造反例并检查直径、最小包围圆和 20 米判据；G 使用随机真值和误差生成器进行覆盖率测试；D/E/F 只反馈接口是否满足策略需要，不得要求 B 为某条策略修改数学定义。

### Agent C：模拟器接口与运行工程

**目标**

建立稳定、可重试、可记录、可在演练和正式测试中复用的通信层。

**任务**

- 核对模拟器版本、端口、登录、系统时间和网络条件；
- 实现四类请求的统一客户端；
- 正确维护坐标、测向频道和虚拟时间；
- 为每个新动作生成唯一 `request_id`；
- 网络异常时以相同请求和相同 ID 幂等重试；
- 同时检查 HTTP 状态和 `accepted`；
- 记录请求、响应、现实时间、虚拟时间和策略版本；
- 提供剩余现实时间保护和安全退出机制；
- 编写演练、正式测试、异常恢复和日志导出运行手册；
- 监测正式日志大小，避免异常高频循环。

**禁止事项**

- 不在客户端中决定下一个检测位置；
- 不因方便而并发发送动作；
- 不用新的 ID 重试同一个不确定结果的请求；
- 不把 `accepted=false` 返回的零虚拟时间当作当前时刻；
- 不擅自启动正式测试。

**输出**

- `02_simulator_interface/CLIENT_SPEC.md`
- `shared/PROTOCOL_CONTRACT.md`
- `02_simulator_interface/RUNBOOK.md`
- `02_simulator_interface/FAILURE_RECOVERY.md`
- 统一客户端、最小连通测试和日志解析器。

**交叉验证**

H 通过断网、超时、重复请求、错误频道和连接关闭测试客户端；G 对比影子模拟器与客户端记录的虚拟时间；A 只根据验收报告批准进入正式测试。

### Agent D：问题三主策略

**目标**

独立建立全向干扰源的完整在线策略，在保证不漏源的前提下减少移动、检测、切换和失败清除时间。

**必须覆盖**

- 20 个频道的状态管理；
- 覆盖检测点或其他可证明的频道发现机制；
- `no_signal` 的安全解释；
- 示向度约束的累积更新；
- 下一检测点选择；
- 何时进入 20 米清除阶段；
- 多频道共享检测点；
- 多目标清除路径；
- 清除失败后的恢复；
- 总目标数未知时的停止证书；
- 现实时间不足时的安全回退策略。

**开发顺序**

1. 建立一定能结束、但可能较慢的安全基线；
2. 对移动、检测和切换时间进行分项统计；
3. 一次只增加一种优化；
4. 每次优化必须证明不降低全部清除成功率；
5. 在公共训练案例上完成第一版后冻结。

**输出**

- `03_q3_primary/POLICY_SPEC_V1.md`
- `03_q3_primary/STATE_MACHINE.md`
- `03_q3_primary/STOPPING_CERTIFICATE.md`
- `03_q3_primary/RESULTS_PUBLIC.json`
- `03_q3_primary/SUMMARY.md`
- 策略代码、配置、运行日志和消融实验。

**盲隔离**

第一版冻结前不得读取 E 的模型、代码、提示词或结果。第一版必须先登记状态表示、选点目标、停止条件和主指标，之后才能进入交叉阅读。

### Agent E：问题三独立备选策略

**目标**

在与 D 相同的题意、接口和指标下，建立结构不同的全向源策略，避免团队过早收敛到单一路线。

**任务**

- 独立建立频道发现、定位、清除、路径和停止流程；
- 至少在状态表示、选点原则或全局调度方式上与 D 有实质差异；
- 提供自己的安全基线；
- 明确最坏情形和失败恢复；
- 在同一公共案例上输出分项时间；
- 在第一版中记录未采用其他方法的理由，但不得猜测 D 的方案。

可探索的方向包括确定性可行域、概率信念、信息增益、批量扫描、层次式搜索、联合路径规划等。Agent A 不指定 E 必须使用哪一种，以免人为限制思路。

**输出**

- `04_q3_alternative/POLICY_SPEC_V1.md`
- `04_q3_alternative/STATE_MACHINE.md`
- `04_q3_alternative/STOPPING_CERTIFICATE.md`
- `04_q3_alternative/RESULTS_PUBLIC.json`
- `04_q3_alternative/SUMMARY.md`
- 策略代码、配置、日志和消融实验。

**盲隔离与交叉阅读**

D/E 同时冻结第一版后才允许互读。互读后形成 V2，必须保留 V1，并逐项标出新增内容来自独立发现、验证反馈还是对方方案。

### Agent F：问题四定向源策略

**目标**

在问题三能力基础上解决定向源，重点防止将背向检测导致的无信号误判为频道不存在。

**任务**

- 建立位置、有效半径和定向方向的联合状态表示；
- 区分有信号、无信号和近距离反馈的约束含义；
- 设计来自不同方位的探测方式；
- 分析位于圆域边界且向外发射的极端目标；
- 利用机器狗可以移动到目标圆域之外的规则；
- 证明或实验评估发现任意方向定向源的覆盖能力；
- 收到首次信号后复用几何模块快速定位；
- 位置已经满足 20 米保证时停止估计发射方向；
- 设计常规策略失败后的保守回退扫描；
- 分别报告全向源和定向源的发现率与时间。

**禁止事项**

- 不把一次或少量 `no_signal` 当作不存在证据；
- 不默认定向源朝向目标圆域内部；
- 不为了估计精确方向而延迟已经可以保证成功的清除；
- 不未经验证直接复制 D 或 E 的停止条件。

**输出**

- `05_q4_directional/DIRECTIONAL_MODEL.md`
- `05_q4_directional/POLICY_SPEC.md`
- `05_q4_directional/COVERAGE_ARGUMENT.md`
- `05_q4_directional/FALLBACK_POLICY.md`
- `05_q4_directional/RESULTS_PUBLIC.json`
- 策略代码、配置、日志和定向专项实验。

**交叉验证**

G 生成不同定向比例、朝向、边界位置和接收半径案例；H 专门构造“所有内部检测点都处在背面”等反例；B 检查有信号后的几何定位是否仍满足契约。

### Agent G：离线仿真与基准评测

**目标**

提供不依赖有限正式测试次数的可重复实验环境，并对候选策略进行公平盲测。

**任务**

- 按题面规则建立影子模拟器；
- 随机生成源数量、频道、位置、接收半径、类型和方向；
- 模拟固定地点误差，而不是把每次测量误差独立重抽；
- 实现移动、切换、检测、清除和退出的精确计时；
- 建立公共训练集、公开验证集和封闭测试集；
- 实现至少一个安全覆盖基线和一个简单贪心基线；
- 批量运行 D、E、F 的冻结策略；
- 输出均值、中位数、P90、P95、最坏值和置信区间；
- 按源数量、边界距离、接收半径、定向比例和朝向分层统计；
- 对比真实模拟器演练日志，校准影子模拟器的协议和计时。

**隔离要求**

- 不向 D/E/F 暴露封闭案例真值、种子或失败案例坐标；
- 调参阶段只能使用公共案例；
- 策略冻结后才运行封闭测试；
- 若封闭测试失败，A 决定是否解冻。解冻后原封闭集降级为公开集，G 必须新建封闭集。

**输出**

- `06_benchmark/SHADOW_SIMULATOR_SPEC.md`
- `06_benchmark/CASE_GENERATION.md`
- `06_benchmark/BASELINES.md`
- `06_benchmark/BLIND_TEST_REPORT.md`
- `06_benchmark/STRATIFIED_METRICS.csv`
- 影子模拟器、案例生成器和批量评测程序。

**完成标准**

相同动作序列在影子模拟器和真实演练模拟器中的虚拟时间计算一致；候选策略的比较使用相同案例、指标和资源限制。

### Agent H：独立验证与红队

**目标**

主动寻找会导致定位错误、漏源、清除失败、死循环、时间账错误或正式测试事故的反例。

**几何必查**

- 跨 0 度的角度区间；
- 近平行示向线；
- 空交集、无界和退化定位区域；
- 多边形直径圆不能覆盖的反例；
- 最小包围圆的数值误差；
- 多边形满足直径小于 40 米但仍不能被半径 20 米圆覆盖的情况。

**策略必查**

- 同一地点重复测量被错误当作独立信息；
- 一个点无信号就宣布频道不存在；
- 已清除频道仍进入搜索队列；
- 目标总数未知时提前退出；
- 多源靠近、全部靠近边界或接收半径均取最小值；
- 问题四定向源朝外或所有常规测点位于背面；
- 清除失败后状态未恢复；
- 策略在极端反馈序列中循环。

**工程必查**

- 网络中断后的幂等重试；
- 重复 ID 和并发请求；
- `accepted=false`、HTTP 409、接口关闭和超时；
- 虚拟时间独立对账；
- 剩余现实时间不足时能否安全退出；
- 正式日志命名、大小和保存。

**输出**

- `07_validation/VALIDATION_REPORT.md`
- `07_validation/GEOMETRY_COUNTEREXAMPLES.md`
- `07_validation/POLICY_FAILURE_CASES.md`
- `07_validation/PROTOCOL_FAULT_TEST.md`
- `07_validation/TIME_RECONCILIATION.csv`
- 对 `CLAIM_EVIDENCE.csv` 填写通过、有条件通过或驳回。

**完成标准**

最终策略至少通过随机盲测、边界专项测试、协议故障测试、时间对账和简单基线比较；任何未解决漏源案例都必须阻止正式测试批准。

### Agent I：论文、图表与提交

**目标**

把经过验证的几何模型、在线策略、仿真结果和正式测试结果组织成清楚、可复核的论文和支撑材料。

**任务**

- 建立摘要、问题重述、假设、符号、模型、算法、实验、优缺点和参考文献结构；
- 问题一展示示向扇形、定位多边形、直径和最小包围圆；
- 问题二展示交会角、候选区域和选点目标；
- 问题三展示频道状态机、策略流程和时间分解；
- 问题四展示定向覆盖、无信号歧义和多方位搜索；
- 报告基线、候选策略、盲测和分层实验；
- 正确填写六次正式测试结果并关联原始日志；
- 检查论文、支撑材料、程序、日志、AI 使用说明和文件命名；
- 独立渲染最终 PDF，检查乱码、公式、图表和分页。

**禁止事项**

- 不自行运行模型并选择最好看的结果；
- 不创造未注册数字；
- 不把演练结果写成正式测试结果；
- 不隐藏失败案例或把经验表现写成数学保证；
- 不改动正式日志。

**输出**

- `08_paper_submission/PAPER_DRAFT.docx` 或论文源文件；
- `08_paper_submission/FIGURE_INDEX.md`；
- `08_paper_submission/CLAIM_USAGE.md`；
- `08_paper_submission/SUBMISSION_CHECKLIST.md`；
- `08_paper_submission/AI_USAGE_LOG.md`；
- `08_paper_submission/FINAL_FORMAT_AUDIT.md`。

**交叉验证**

B 检查几何公式；D/E/F 检查策略表述；C 检查接口和时间规则；G 检查实验数字；H 检查结论边界；A 只核对状态和证据完整性。

## 8. 关键交叉验证矩阵

| 被检查事项 | 主负责人 | 独立复核 | 判定方式 |
|---|---|---|---|
| 四问题意和指标 | B | D、E、F、H | 列出歧义，由 A 冻结契约版本 |
| 定位区域和直径 | B | H、G | 解析案例、随机真值和反例三类测试 |
| 最小包围圆与清除判据 | B | H | 顶点独立计算并构造边界案例 |
| 第二检测点策略 | B | D、E、G | 理论目标与仿真时间共同评估 |
| HTTP 协议与幂等重试 | C | H | 故障注入和动作状态检查 |
| 虚拟时间计算 | C | G、H | 同一动作序列三方独立对账 |
| 问题三主策略 | D | E、G、H | 盲测、基线和反例测试 |
| 问题三备选策略 | E | D、G、H | 同一封闭案例公平比较 |
| 问题三停止证书 | D/E | H | 构造 10 至 16 个源和缺失频道案例 |
| 问题四定向源策略 | F | G、H、B | 朝向、边界和无信号专项测试 |
| 影子模拟器规则 | G | C、H | 与题面和真实演练日志对账 |
| 正式测试代码版本 | C | G、H、A | 哈希、配置和运行手册一致 |
| 论文数字和结论 | I | G、H | 匹配结果注册表和证据表 |
| 正式日志和提交包 | I | C、A | 按清单逐项核对 |

至少保留以下双重验证：

1. D/E 对问题三进行盲并行；
2. B/H 独立检查定位区域、直径和最小包围圆；
3. C/G/H 三方独立核对虚拟时间；
4. F/H 独立寻找定向源漏检反例；
5. G 使用封闭案例测试冻结策略；
6. I 写入的每个数字由 G 对账、H确认解释边界。

## 9. 工作阶段和闸门

### 阶段 0：环境就绪

C 下载并启动模拟器，跑通最小接口流程；A 建立任务板和正式测试台账；I 固化规则与截止时间。

**闸门 0**：模拟器能登录，接口可连接，系统时间正确，日志可保存，正式测试次数保持未使用。

### 阶段 1：契约冻结

B 完成问题和几何契约；C 完成协议契约；G 完成公共案例格式；H 提交首轮歧义与风险清单。

**闸门 1**：角度、时间、状态、指标和策略接口定义一致。

### 阶段 2：盲并行开发

B 完成问题一和问题二；D/E 独立开发问题三 V1；F 独立分析问题四；G 建立影子模拟器和公共基线。

**闸门 2**：D/E 同时冻结 V1 后才可互读。所有路线均能在公共案例上完成运行。

### 阶段 3：集成和演练

C 将策略接入真实模拟器；G 对比影子与真实演练；H 执行几何、协议和策略红队测试。

**闸门 3**：没有时间账差异、协议错误、死循环或已知漏源案例。

### 阶段 4：优化和消融

D/E/F 根据公开验证结果形成 V2；G 统一比较；H 检查每项优化是否降低可靠性。

**闸门 4**：确定问题三主策略、问题三保底策略、问题四主策略和统一回退条件。

### 阶段 5：代码冻结与封闭盲测

记录代码哈希和配置。G 在封闭案例上批量测试，H审查失败案例但不向策略作者泄露真值。

**闸门 5**：全部清除成功率、P95时间、异常退出和接口稳定性达到指标契约中的准入线。

### 阶段 6：正式测试

A 核对批准记录，C 按运行手册操作，H 旁路监控但不临时改策略，I 保存案例编码、结果和原始日志。

**闸门 6**：问题三和问题四各三次正式测试均完成，日志进入上传队列并完成本地备份。

### 阶段 7：论文和提交

I 根据核验结果完成论文和支撑材料；各技术角色只审阅自己负责部分；A 组织最后一次证据核对。

**闸门 7**：论文 PDF、支撑程序、六份正式日志、AI 使用说明和提交包全部通过清单。

## 10. 正式测试机会管理

正式测试不是调试环境。每次启动前必须在 `FORMAL_TEST_LEDGER.md` 登记：

```text
question: Q3 / Q4
attempt: 1 / 2 / 3
policy_version:
code_hash:
config_hash:
simulator_version:
operator: Agent C
approval: Agent A
validation_report:
start_time:
case_code:
end_reason:
program_runtime:
clear_count:
average_clear_time:
log_path:
upload_status:
```

正式测试前必须满足：

- 使用冻结代码和冻结配置；
- 最小连接测试已通过；
- 机器人编号、端口和系统时间已核对；
- 现实时间保护和 `/exit` 路径已测试；
- 不存在未关闭的调试程序或并发客户端；
- 日志目录有足够空间；
- 操作人员明确知道何时不得手工中止；
- A 已在台账中记录批准，C 才能启动。

正式测试过程中不得临时改代码。出现网络异常时只能按 C 的幂等重试手册处理。

## 11. Agent 间提问协议

每次跨 Agent 提问只处理一个可判定问题：

```text
问题编号：Q-角色-序号
提出者：
接收者：
关联小问：
当前契约版本：
当前已知事实：最多 5 条
需要回答的唯一问题：
期望输出：公式 / 判断 / 测试 / 文件
截止闸门：
证据文件：
```

接收者不得要求完整聊天历史。上下文不足时，只指出缺少的具体文件、字段或契约条款。

## 12. Agent A 可直接复制的任务模板

### 12.1 启动任务模板

```text
你是 Agent {角色字母}，职责是：{一句话职责}。

只完成本角色任务，不承担其他角色工作。
允许读取：{输入文件}。
允许修改：{输出目录}。
禁止读取：{隔离目录}。

当前问题契约版本：{版本号}。
当前协议契约版本：{版本号}。
当前指标契约版本：{版本号}。
本阶段截止闸门：{编号}。

必须输出：
1. SUMMARY.md；
2. {角色专属文件}；
3. 复现命令或证据路径；
4. 假设、失败条件和未解决问题；
5. 给下游 Agent 的最小文件索引。

不得把演练经验写成数学保证。完成后只向 Agent A 返回摘要和文件索引，不粘贴长代码或完整日志。
```

### 12.2 独立复核模板

```text
请独立复核 claim_id={编号}。
原结论：{结论}。
允许读取：{最小证据文件}。
禁止把原作者解释作为前提。

输出：
- 复现是否成功；
- 使用的独立方法；
- 数值差异；
- 发现的反例或协议问题；
- 状态：通过 / 有条件通过 / 驳回；
- 若驳回，给出最小修复建议。
```

### 12.3 D/E 冲突裁决模板

```text
D 与 E 在 {具体决策或结论} 上存在冲突。
禁止投票、取平均或按模型复杂度决定。

请 G 使用同一封闭案例和指标契约进行盲测，请 H 判断差异来自题意、几何、接口、随机性、停止条件还是策略本身。A 只根据全部清除成功率、尾部时间、平均时间和可复现性记录决定。
```

### 12.4 正式测试批准模板

```text
拟运行：问题 {Q3/Q4} 正式测试第 {次数} 次。
策略版本：{版本}。
代码哈希：{哈希}。
验证报告：{路径}。
运行手册：{路径}。
剩余机会：{数量}。

请 C 逐项执行运行手册。除手册规定的幂等重试外，不修改代码、配置、请求或日志。测试结束后立即记录案例编码、结果、结束原因和日志路径，不对日志做任何修改。
```

## 13. Agent A 的任务看板字段

`TASK_BOARD.md` 建议使用：

| task_id | question | owner | contract_version | input | output | dependency | status | reviewer | gate | blocker |
|---|---|---|---|---|---|---|---|---|---|---|

状态只使用：

- `未开始`
- `进行中`
- `待复核`
- `需返工`
- `已通过`
- `已冻结`

任务进入“已通过”前必须有独立 reviewer。冻结任务若被修改，A 必须把受影响的下游任务重新置为“待复核”。

## 14. 推荐内部准入线

以下指标用于决定是否允许进入正式测试，不代表官方评分线：

- 公开案例和封闭案例均不存在已知死循环；
- 封闭案例全部清除成功率不低于 99.5%，且最终人工审查集中不出现漏源；
- 第一次清除成功率接近 100%；
- 虚拟时间账与独立计算误差为 0；
- P95 平均清除时间较安全覆盖基线有明确改善；
- 问题四在不同定向比例、不同方向和边界源分层中均无系统性漏检；
- API 异常能够安全重试或退出；
- 现实运行时间留有安全余量；
- 日志大小低于限制并能正确导出。

若两个策略都达到 100% 全部清除，优先选择 P95 时间更短者；P95 接近时再比较平均时间。若快速策略存在少量漏源，而保底策略没有漏源，正式测试使用保底策略。

## 15. 最终验收清单

### 题目覆盖

- 问题一给出定位区域算法、直径算法和圆覆盖结论；
- 问题二给出第二点候选区域、评价指标和选择算法；
- 问题三包含发现、定位、清除、调度、失败恢复和停止条件；
- 问题四明确处理定向源和无信号歧义；
- 各问使用相同的角度、距离和时间定义。

### 几何与策略

- 示向度误差没有被当作独立随机重复测量；
- 20 米保证使用最小包围圆或等价严谨判据；
- 一次无信号没有被错误解释为频道不存在；
- 总目标数未知时有可审查的停止证书；
- 定向源边界朝外等极端情况已测试；
- 清除失败后不会丢失该频道或无限循环。

### 软件与实验

- 模拟器客户端和策略代码分离；
- 影子模拟器与真实演练时间一致；
- 公共调参与封闭测试严格分开；
- 随机种子、代码哈希、配置和环境已记录；
- 所有最终表图能由统一命令重新生成；
- 六次正式测试的案例编码、结果和原始日志完整；
- 正式日志已上传并完成本地只读备份。

### 论文与提交

- 摘要包含具体方法、正式结果和验证方式；
- 数学保证与经验结果明确区分；
- 时间指标与官方定义一致；
- 图表包含单位、样本量和策略版本；
- 正文数字与结果注册表一致；
- 支撑材料包含程序、说明和要求的日志；
- AI 使用记录、引用、匿名和格式符合当年规范；
- 最终 PDF 无乱码、遮挡、空白页和编号错误。

## 16. 防止思路收敛和相关错误的硬规则

1. D/E 第一版冻结前保持完全隔离；
2. 每条策略先登记状态、动作、停止条件和评价指标，再查看结果；
3. G 不公开封闭测试真值和种子；
4. H 的任务是找失败案例，不是替主策略解释失败；
5. A 在盲测前不公开自己偏好的策略；
6. 问题四不得默认沿用问题三的无信号解释；
7. 多个 Agent 给出相同答案不能作为正确证据，共用模型可能产生相关错误；
8. 所有“显著更快”的结果都必须检查是否牺牲清除率或提前退出；
9. 所有几何保证必须有解析证明或覆盖性测试，不能只靠示意图；
10. 所有优化都与安全基线比较，并保留失败回退路径；
11. 正式测试失败后不得立刻凭猜测修改下一次，必须先由 C/H 定位原因并由 A 记录决定；
12. 论文保留主策略、备选策略、选择依据和已知限制，不掩盖不确定性。

这套安排的重点是让 Agent A 管理任务、证据、版本和正式测试机会。技术 Agent 只交付边界明确、可复现的局部成果；几何、策略、接口和结果分别经过独立复核后，才允许进入正式测试和论文。
